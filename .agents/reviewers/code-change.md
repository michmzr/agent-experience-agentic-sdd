# Reviewer: code and change behavior

Analyze change churn and implementation behavior visible in the session.

Look for code written and removed due to weak repo understanding, unnecessary refactors, repeated rework after review, wrong abstraction choices and changes that solved symptoms rather than root causes.

Separate agent behavior problems from genuine architectural friction in the repository.
